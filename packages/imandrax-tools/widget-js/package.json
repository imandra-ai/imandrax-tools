{
  "name": "imandrax-api-widget",
  "version": "0.1.0",
  "description": "Read-only visualization widgets for imandrax-api dataclasses.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build:treemap && npm run build:icicle && npm run build:task",
    "build:treemap": "esbuild src/region_decomp/index.ts --bundle --format=esm --minify --outfile=dist/region_decomp.js",
    "build:icicle": "esbuild src/region_decomp/icicle_entry.ts --bundle --format=esm --minify --outfile=dist/region_decomp.icicle.js",
    "build:task": "esbuild src/task/index.ts --bundle --format=esm --minify --outfile=dist/task.js",
    "dev": "esbuild src/region_decomp/index.ts --bundle --format=esm --watch --outfile=dist/region_decomp.js",
    "dev:task": "esbuild src/task/index.ts --bundle --format=esm --watch --outfile=dist/task.js",
    "gallery": "node scripts/gallery/build_gallery.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "d3-hierarchy": "^3.1.2",
    "d3-scale": "^4.0.2",
    "d3-scale-chromatic": "^3.1.0",
    "d3-selection": "^3.0.0"
  },
  "devDependencies": {
    "@types/d3-hierarchy": "^3.1.7",
    "@types/d3-scale": "^4.0.9",
    "@types/d3-scale-chromatic": "^3.1.0",
    "@types/d3-selection": "^3.0.11",
    "esbuild": "^0.25.0",
    "jsdom": "^25.0.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.0"
  }
}
