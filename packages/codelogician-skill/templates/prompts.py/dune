(rule
  (mode (promote (until-clean)))
  (deps
    (:templ prompts.py.jinja)
    (:context ../../knowledge-base/context.yaml))
  (target prompts.py)
  (action (
      run jinja -d %{context} -o %{target} %{templ}
    )
  )
)
