(rule
 (targets constants.yaml)
 (mode
  (promote (until-clean)))
 (deps
  (source_tree src)
 )
 (action
  (with-stdout-to
   %{targets}
   (run dump-of-fs src --format yaml --no-default-ignore)
   )))
