- Each folder represents a "library" of IML modules.

- Convention:
   - use `main.iml` for the module for the entry point (terminal object)
   - we should be able to check the import of `main.iml` to see if the library is well-formed
