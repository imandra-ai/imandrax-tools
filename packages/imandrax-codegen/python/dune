(subdir py_gen
  (rule
    (target art_parse.exe)
    (deps ../../bin/parse.exe)
    (mode promote)
    (action (copy %{deps} %{target})))

  (alias
    (name python-setup)
    (deps art_parse.exe)))
