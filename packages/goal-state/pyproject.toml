[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "goal-state"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
features = ["python"]
module-name = "imandrax_tools.goal_state"
