[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "goal-state"
requires-python = ">=3.9"
dynamic = ["version"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.maturin]
features = ["python"]
module-name = "goal_state"

[tool.pyright]
venvPath = ".venv"
